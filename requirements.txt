kokoro>=0.9.4
soundfile>=0.12
spacy>=3.7
numpy>=1.24

# torch is installed separately so you can pick the right build — see README.
#   CPU / modern GPU:   pip install torch
#   Maxwell GPU:        pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
